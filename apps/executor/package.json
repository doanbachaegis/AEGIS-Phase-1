{
  "name": "@aegis/executor",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "aegis-settle": "./dist/cli.js"
  },
  "scripts": {
    "dev": "tsx watch src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "settle": "node dist/cli.js"
  },
  "dependencies": {
    "@aegis/bindings": "workspace:*",
    "@aegis/canonical": "workspace:*",
    "@aegis/receipt": "workspace:*",
    "@stellar/stellar-sdk": "^16.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.1"
  }
}
