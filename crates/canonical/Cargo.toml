[package]
name = "aegis-canonical"
version.workspace = true
edition.workspace = true

[dependencies]
sha2 = { version = "0.10", default-features = false }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
